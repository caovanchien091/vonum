import 'package:common/common.dart';
import 'package:flutter/material.dart';

class WidgetRoute extends RouteRegister {
  @override
  Route? generate(settings, injection) {
    switch (settings.name) {
    }
  }
}
